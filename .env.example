# Optional. Defaults to INFO. Use DEBUG to see Gmail API request details.
MGDIO_LOG_LEVEL=INFO

# Optional. Set to 1 to run the opt-in integration tests that hit real
# Google APIs (see tests/gmail/test_integration.py). Leave unset normally.
MGDIO_RUN_INTEGRATION=
